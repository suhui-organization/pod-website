# Product

## Register

brand

> This file describes the **marketing site**. The product console (`pod ui`) has
> its own register and design system in the main repo at
> `../SecurityHarness/pod/PRODUCT.md` and `../SecurityHarness/pod/DESIGN.md`.
> Where the two touch — the cobalt primary, the `allow / approve / deny`
> vocabulary — the site follows the product, not the other way around.

## Users

**主要**：出海 OPC / 独立开发者。本机跑 2–5 个 agent（Claude Code、Cursor、OpenClaw、Codex）
和 3–15 个 MCP server，自己既是运维也是安全负责人。他们**没有**安全团队，也**不打算**读
一份 40 页的白皮书。

**次要**：2–10 人微型工作室的技术负责人，需要向客户交代「这个 agent 能碰什么、做过什么」。

**次要**：从 Gitee 过来的国内开发者。网络环境决定了 GitHub 主源可能连不上——
安装路径必须把 Gitee 放在第一位。

**两拨读者的语言不同。** 主受众读中文，但项目本身（README、docs、CLI 输出）是中英并行的，
而且 pod 面向的是出海开发者。所以页面必须双语：默认跟随浏览器语言，导航栏一键切换，
选择记在本地。不是「中文站加一个英文页脚」，是两套完整对等的文案。

**使用情境**：工作日下午，浏览器处于白天光照环境，旁边就是编辑器和终端。他们刚在
GitHub 或 Gitee 上看到这个项目，或者在某个群里被安利，现在想用 **30 秒**判断两件事——
这东西解决的是不是我真实的问题；值不值得为它装一个 Node 依赖。

**要完成的任务**：理解「从真实行为编译策略」和「手写策略的网关」的区别，然后决定装不装。

## Product Purpose

这是 pod 的项目介绍页。pod 是一款为 AI agent 编译最小权限策略的工具。

它不承担转化漏斗的职责，不收集邮箱，不引导注册。它只有一个目标：**让一个不熟悉 pod 的
开发者在上半屏之内理解它的独特性，并能立刻在本地跑起来。**

成功的信号：访客能复述出「它不是又一个 MCP 网关，它从真实行为里编译策略」这句话，
并且安装命令是页面上最容易找到的东西。

## Brand Personality

克制、可信、可核查。安全工具的自信来自把证据摆出来，而不是来自形容词。

语气像一份被认真维护的台账：安静、精确、不恐吓、不营销腔。术语保留英文原文
（agent / server / tool / policy / deny / record / compile）。

三条语音规则：

- **说清楚不夸大。** 缺数据就写「未记录」，不编造，不用模糊的「安全 / 风险」标签掩盖信息缺口。
- **坏消息也说出来。** 页面主动用一整节讲 pod 不做什么。这是可信度的来源，不是弱点。
- **不恐吓。** 不写「你的 agent 正在泄露数据」。写清楚机制，让读者自己下结论。

## Anti-references

- **恐吓式安全仪表盘**：满屏红色、`RISK SCORE 87` 大字、没有出处的结论。
- **通用 SaaS 落地页**：三栏一样的图标卡片、「Why choose us」、假客户 logo 墙、
  用色块占位的假截图。
- **终端崇拜**：为了「极客感」牺牲可读性（通篇等宽、荧光绿、纯黑底）。
- **AI 生成感**：大号渐变文字、玻璃拟态卡片、满屏 `border + 大范围投影`、
  24px 以上的圆角、手绘风 SVG 插图、每个 section 上方一条小号全大写 eyebrow 标签。
- **发号施令式文案**：「立刻保护你的 AI！」这类祈使句。页面给证据，不下命令。

## Design Principles

1. **证据优先，结论其次。** 页面上每个判定都要能指回它的出处。首屏那 24 个能力格子和
   中间的策略 diff 都不是截图，是现场算出来的——点开任意一行能看到「哪一次调用导致了这个结论」。
2. **先讲人话，再给命令。** 每个技术概念都要有一句不用术语的解释，命令紧随其后。
   「从语料编译最小权限规则」是准确的，但没人看得懂；「先看它实际用过什么，再自动写出
   一份权限清单」才是这一页该说的话。
3. **一个签名时刻，而不是每屏都动。** 动效预算全部花在首屏那一次「策略被编译出来」上。
   其余区块不做统一的淡入上浮——那是 AI 生成页面的语法，不是编排。
4. **功能要在第二屏内看全。** 访客第一眼要有视觉冲击，第二眼就要知道「这东西都能干什么」。
   能力清单排在 hero 正下方，用八块大小不一的方块呈现，每块以命令收尾——开发者扫命令
   比扫散文快得多。剩下十几个命令再用一行列全，保证清单完整。机制（怎么做到的）
   排在能力之后：先讲能得到什么，再讲怎么做到。
5. **一句话能说清的，不放三张卡片。** 卡片是最懒惰的答案。只有当一个信息块真的需要
   独立边界时才给边界，否则用留白和发丝线分组。
6. **安装命令是最重要的元素。** 首次访问可能只停留 30 秒，那 30 秒应该花在复制命令上。
7. **三个判定永远带文字。** `allow` / `approve` / `deny` 从不仅靠颜色区分——这是 pod
   自己的可访问性约定，介绍页也遵守。
8. **中文为主，术语保留英文。** 受众是中文开发者，但 agent / policy / deny 这些词
   翻译过来反而增加理解成本。
9. **两个源站同等对待。** GitHub 和 Gitee 都是一等公民，不是脚注里的「镜像」。

## Accessibility & Inclusion

- **WCAG 2.1 AA**：正文对比度 ≥ 4.5:1，非文本元素 ≥ 3:1。所有颜色值在构建时用
  WCAG 公式（OKLCH → sRGB → 相对亮度）核算过，并在 tokens 里标注比值；渲染后又用
  canvas 合成像素复核过一遍，浅色区和深色区各自验证。
- **状态不只靠颜色**：三态判定同时带文字与形状标记（圆点 / 方块 / 斜方块 / 双横线）。
  首屏那 24 个能力格子用形状加底色区分，配一条图例解码；每个格子另有屏幕阅读器文本。
- **`prefers-reduced-motion: reduce`**：关闭全部进场动画与过渡，首屏编译动画与证据链
  直接呈现最终状态，内容始终可见。
- **键盘可达**：跳转到主内容的 skip link 是第一个 Tab 停留点，所有可交互元素有
  2px 可见 focus 环。
- **语言切换是完整的状态变更**：`<html lang>`、`<title>`、meta description、skip link、
  所有正文与图表标签一起切换，并持久化到 `localStorage`。英文文案比中文长约 30%，
  所有断点都在两种语言下各自验证过溢出与截断。
- **CJK 优先的字体策略**：拉丁字形自托管，中文回退到系统字体（PingFang SC 等），
  避免依赖在中国大陆无法访问的字体 CDN。
